<Window x:Class="MaxTemp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MaxTemp"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" ResizeMode="NoResize">

	<Window.Resources>
		<!-- Farbressourcen -->
		<SolidColorBrush x:Key="WindowBackgroundColor" Color="#F5F5F5"/>
		<SolidColorBrush x:Key="AccentColor" Color="#007ACC"/>

		<!-- Button Style -->
		<Style x:Key="ButtonStyle" TargetType="Button">
			<Setter Property="FontSize" Value="14"/>
			<Setter Property="Padding" Value="10,5"/>
			<Setter Property="Background" Value="#007ACC"/>
			<Setter Property="Foreground" Value="White"/>
			<Setter Property="BorderBrush" Value="#007ACC"/>
			<Setter Property="BorderThickness" Value="2"/>
			<Setter Property="FontWeight" Value="Bold"/>
			<Setter Property="Cursor" Value="Hand"/>
			<Setter Property="Height" Value="35"/>
			<Setter Property="Margin" Value="5"/>
			<Setter Property="HorizontalAlignment" Value="Center"/>
			<Setter Property="VerticalAlignment" Value="Center"/>
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="Button">
						<Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5">
							<ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
						</Border>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
		</Style>

		<!-- DatePicker Style mit Icon -->
		<Style x:Key="DatePickerWithIconStyle" TargetType="DatePicker">
			<Setter Property="Width" Value="150"/>
			<Setter Property="FontSize" Value="14"/>
			<Setter Property="Padding" Value="5"/>
			<Setter Property="HorizontalAlignment" Value="Center"/>
			<Setter Property="VerticalAlignment" Value="Center"/>
			<Setter Property="Background" Value="#FFFFFF"/>
			<Setter Property="BorderBrush" Value="#D1D1D1"/>
			<Setter Property="BorderThickness" Value="1"/>
		</Style>

		<!-- Label Style -->
		<Style x:Key="TextLabelStyle" TargetType="Label">
			<Setter Property="FontSize" Value="16"/>
			<Setter Property="HorizontalAlignment" Value="Center"/>
			<Setter Property="VerticalAlignment" Value="Center"/>
			<Setter Property="Margin" Value="10"/>
			<Setter Property="Foreground" Value="#333333"/>
		</Style>
	</Window.Resources>

	<Viewbox HorizontalAlignment="Center" VerticalAlignment="Center">
		<Grid HorizontalAlignment="Center" VerticalAlignment="Center">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="1*" />
				<ColumnDefinition Width="Auto" />
			</Grid.ColumnDefinitions>

			<Grid.RowDefinitions>
				<RowDefinition Height="Auto" />
				<RowDefinition Height="Auto" />
				<RowDefinition Height="1*" />
				<RowDefinition Height="Auto" />
			</Grid.RowDefinitions>

			<!-- Titel und Auswahl der beiden Daten (Start- und Enddatum) ganz oben -->
			<Label Grid.Row="0" Content="Wählen Sie ein Zeitraum:" Style="{StaticResource TextLabelStyle}"/>

			<StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10">
				<!-- Erster DatePicker für Startdatum mit Icon -->
				<DatePicker Name="startDatePicker" Style="{StaticResource DatePickerWithIconStyle}" />
				<TextBlock Text="bis" VerticalAlignment="Center" Margin="5" FontSize="14"/>
				<!-- Zweiter DatePicker für Enddatum mit Icon -->
				<DatePicker Name="endDatePicker" Style="{StaticResource DatePickerWithIconStyle}" />
			</StackPanel>

			<!-- Aufforderung und Button (nun weiter unten) -->
			<Grid Grid.Row="1" Grid.ColumnSpan="2" HorizontalAlignment="Center" VerticalAlignment="Center">
				<StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
					<Label Name="lblAufforderung" Style="{StaticResource TextLabelStyle}" Content="Bitte klicken, um Temperaturen auszuwerten."/>
					<Button Name="btnAuswerten" Style="{StaticResource ButtonStyle}" Click="BtnAuswerten_Click" Content="Auswerten"/>
				</StackPanel>
			</Grid>

			<!-- Ausgabe-Label ganz zentriert -->
			<Label Grid.Row="2" Grid.ColumnSpan="2" Name="lblAusgabe" Style="{StaticResource TextLabelStyle}" Content="---Warte auf Auswertung---" />
		</Grid>
	</Viewbox>
</Window>